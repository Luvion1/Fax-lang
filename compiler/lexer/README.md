# Lexer Module

This module handles tokenization of Fax source code. It converts raw source code into a stream of tokens that can be processed by the parser.

## Components
- `token.zig`: Defines token types and structures
- `lexer.zig`: Main lexer implementation
- `error.zig`: Error handling for lexical analysis
# Parser Module

This module handles parsing of Fax source code. It takes a stream of tokens from the lexer and constructs an Abstract Syntax Tree (AST).

## Components
- `ast.zig`: Defines AST node types and structures
- `parser.zig`: Main parser implementation
- `grammar.zig`: Grammar rules for Fax language
- `error.zig`: Error handling for syntactic analysis